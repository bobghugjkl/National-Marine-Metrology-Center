# 🌊 海洋调查管理系统 - 完整部署指南

## 📋 项目概述

这是一个基于 Vue 3 + Flask + MySQL 的全栈海洋调查质量监督管理平台，支持任务管理、人员管理、设备管理、航前/航中质量检查等核心功能。

## 🛠️ 环境要求

| 工具 | 版本要求 | 说明 |
|------|---------|------|
| Node.js | >= 16.x | 前端开发环境 |
| Python | >= 3.11 | 后端开发环境 |
| MySQL | >= 8.0 | 数据库服务 |
| Git | 最新版 | 代码版本控制 |

## 🚀 完整部署步骤

### 第一步：环境准备

#### 1.1 安装 Node.js
- 访问 [Node.js官网](https://nodejs.org/) 下载并安装 LTS 版本
- 验证安装：`node --version` 和 `npm --version`

#### 1.2 安装 Python
- 访问 [Python官网](https://www.python.org/) 下载并安装 Python 3.11+
- 验证安装：`python --version`
- 确保 pip 可用：`pip --version`

#### 1.3 安装 MySQL
- 访问 [MySQL官网](https://www.mysql.com/) 下载并安装 MySQL 8.0+
- 启动 MySQL 服务
- 设置 root 密码为：`WASDijkl15963`（或根据实际情况修改）

### 第二步：获取项目代码

```bash
# 克隆项目到本地
git clone <项目仓库地址>
cd vue-manage-system
```

### 第三步：数据库配置

#### 3.1 创建数据库
```sql
-- 登录 MySQL
mysql -u root -p

-- 创建数据库
CREATE DATABASE marine_survey_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- 退出 MySQL
exit;
```

#### 3.2 导入数据库结构和数据
```bash
# 导入完整的数据库备份（推荐）
mysql -u root -p marine_survey_db < marine_survey_db_complete_final.sql

# 或者使用其他SQL文件
mysql -u root -p marine_survey_db < marine_survey_db_latest.sql
mysql -u root -p marine_survey_db < mysql_final.sql
```

### 第四步：后端配置

#### 4.1 安装 Python 依赖
```bash
# 进入后端目录
cd backend

# 安装依赖
pip install -r requirements.txt

# 如果安装失败，使用国内镜像源
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

#### 4.2 配置数据库连接
检查 `backend/config/database.py` 文件中的数据库配置：

```python
DATABASE_CONFIG = {
    'SQLALCHEMY_DATABASE_URI': 'mysql+pymysql://root:WASDijkl15963@localhost:3306/marine_survey_db?charset=utf8mb4',
    'SQLALCHEMY_TRACK_MODIFICATIONS': False,
    'JSON_AS_ASCII': False
}
```

**注意**：如果您的 MySQL 密码不是 `WASDijkl15963`，请修改上述配置。

#### 4.3 启动后端服务
```bash
# 在 backend 目录下执行
python app.py
```

**成功标志**：
- 看到 "统一后端服务正在启动..." 消息
- 访问 http://localhost:5000 可以看到欢迎页面
- 访问 http://localhost:5000/api/health 返回健康检查信息

### 第五步：前端配置

#### 5.1 安装前端依赖
```bash
# 回到项目根目录
cd ..

# 安装依赖（推荐使用 yarn）
yarn install

# 或者使用 npm
npm install
```

#### 5.2 启动前端服务
```bash
# 启动开发服务器
yarn dev
# 或者
npm run dev
```

**成功标志**：
- 前端服务运行在 http://localhost:5173
- 浏览器自动打开项目页面

### 第六步：验证部署

#### 6.1 访问系统
- 前端地址：http://localhost:5173
- 后端地址：http://localhost:5000

#### 6.2 测试登录
使用以下测试账号登录：

| 用户名 | 密码 | 角色 | 说明 |
|--------|------|------|------|
| test | 123456 | 测试用户 | 基础功能测试 |
| admin | 123456 | 超级管理员 | 完整权限 |
| 123 | 123456 | 普通用户 | 包含所有现有任务数据 |

## 🔧 配置说明

### 数据库配置
- **数据库名**：`marine_survey_db`
- **用户名**：`root`
- **密码**：`WASDijkl15963`
- **端口**：`3306`
- **字符集**：`utf8mb4`

### 服务端口
- **前端服务**：`5173` (Vite 开发服务器)
- **后端服务**：`5000` (Flask 应用)

### 重要文件说明
- `backend/config/database.py` - 数据库连接配置
- `src/utils/request.ts` - 前端 API 请求配置
- `marine_survey_db_latest.sql` - 完整数据库备份
- `requirements.txt` - Python 依赖列表
- `package.json` - Node.js 依赖列表

## 🐛 常见问题解决

### 1. 数据库连接失败
**错误**：`Can't connect to MySQL server`

**解决方案**：
```bash
# 检查 MySQL 服务状态
# Windows
net start mysql

# Linux/Mac
brew services start mysql
# 或
sudo systemctl start mysql

# 检查端口占用
netstat -an | grep 3306
```

### 2. Python 依赖安装失败
**错误**：`pip install` 超时或报错

**解决方案**：
```bash
# 使用国内镜像源
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple

# 或使用阿里云镜像
pip install -r requirements.txt -i https://mirrors.aliyun.com/pypi/simple/

# 升级 pip
python -m pip install --upgrade pip
```

### 3. 前端依赖安装失败
**错误**：`npm install` 或 `yarn install` 报错

**解决方案**：
```bash
# 清除缓存
rm -rf node_modules package-lock.json yarn.lock

# 使用 yarn（推荐）
yarn install

# 或使用 npm
npm install --registry https://registry.npmmirror.com
```

### 4. 端口被占用
**错误**：`Port 5000 is already in use`

**解决方案**：
```bash
# 查找占用端口的进程
netstat -ano | findstr :5000

# 杀死进程（Windows）
taskkill /PID <进程ID> /F

# 或修改端口（在 backend/app.py 最后一行）
app.run(host='0.0.0.0', port=5001, debug=True)
```

### 5. 跨域问题
**错误**：`CORS policy: No 'Access-Control-Allow-Origin' header`

**解决方案**：
- 确保后端服务已启动
- 检查 `backend/config/cors.py` 配置
- 清除浏览器缓存

## 📱 生产环境部署

### 使用 PM2 管理 Node.js 进程
```bash
# 安装 PM2
npm install -g pm2

# 启动前端服务
pm2 start "npm run dev" --name "vue-frontend"

# 启动后端服务
pm2 start "python app.py" --name "flask-backend"
```

### 使用 Nginx 反向代理
```nginx
server {
    listen 80;
    server_name your-domain.com;
    
    location / {
        proxy_pass http://localhost:5173;
    }
    
    location /api {
        proxy_pass http://localhost:5000;
    }
}
```

## 📞 技术支持

如果遇到问题，请检查：
1. 所有服务是否正常启动
2. 数据库连接是否正常
3. 端口是否被占用
4. 依赖是否正确安装

## 🎯 快速验证清单

- [ ] MySQL 服务运行正常
- [ ] 数据库 `marine_survey_db` 创建成功
- [ ] 数据库数据导入成功
- [ ] Python 依赖安装完成
- [ ] 后端服务启动成功 (http://localhost:5000)
- [ ] Node.js 依赖安装完成
- [ ] 前端服务启动成功 (http://localhost:5173)
- [ ] 可以正常登录系统
- [ ] 可以查看任务列表
- [ ] 可以创建新任务

完成以上所有步骤后，您的海洋调查管理系统就可以正常运行了！
